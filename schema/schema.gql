type Query {
  welcome: String
}

input ProductInput {
  name: String
  price: Int
}

type Product {
  id: Int
  name: String
  price: Int
}

type Mutation {
  addProduct(product: ProductInput): Product
}
